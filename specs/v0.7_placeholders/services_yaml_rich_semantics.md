# `services.yaml` Rich Semantics

Placeholder for v0.7 P1 work to expand `services.yaml` from command manifest into a richer service contract covering dependencies, ports, health checks, environment policy, and validator-safe lifecycle control.
